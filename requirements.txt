# StemScribe Dependencies (pinned versions from venv311, 2026-03-04)
# Install with: pip install -r requirements.txt

# =============================================================================
# Core Web Framework
# =============================================================================
flask==3.1.3
flask-cors==6.0.2

# =============================================================================
# Audio Separation & Processing
# =============================================================================
demucs==4.0.1                   # Stem separation (htdemucs_6s model)
librosa==0.11.0                 # Audio analysis, chroma features, beat detection
soundfile==0.13.1               # Audio I/O
numpy>=1.26,<2.0             # <2.0 for ABI compat with tensorflow 2.14 (pulled via basic-pitch). Upgrade both together when bumping.
scipy==1.17.1
# pydub                         # Audio format conversion (not currently installed)

# =============================================================================
# ML / Deep Learning
# =============================================================================
torch==2.10.0                   # Neural networks (chord detection, transcription)
torchaudio==2.10.0              # Audio transforms
onnxruntime==1.24.2             # ONNX model inference

# =============================================================================
# Music Transcription & Notation
# =============================================================================
basic-pitch==0.4.0              # MIDI transcription (Spotify)
music21==9.9.1                  # MusicXML generation, key detection, notation
mido==1.3.3                     # MIDI file handling
pretty-midi==0.2.11             # MIDI analysis
pyguitarpro==0.10.1             # Guitar Pro (.gp5) export

# =============================================================================
# YouTube / URL Downloads
# =============================================================================
yt-dlp                          # YouTube, Spotify, SoundCloud, Bandcamp (unpinned — updates frequently)

# =============================================================================
# Google Drive Integration
# =============================================================================
google-auth-oauthlib
google-api-python-client
google-auth-httplib2

# =============================================================================
# Optional - Enhanced Features (graceful fallback if missing)
# =============================================================================
# pedalboard                    # Stem audio enhancement (Spotify)
# noisereduce                   # Noise reduction for stems
# pyloudnorm                    # LUFS loudness normalization
# audio-separator               # BS-Roformer vocal separation
# psutil                        # System memory monitoring for GPU manager
# oemer                         # Deep learning OMR (sheet music recognition)

# =============================================================================
# HTTP & API
# =============================================================================
requests==2.32.5                # MusicBrainz, Wikipedia API calls

# =============================================================================
# Authentication & Security
# =============================================================================
flask-jwt-extended==4.7.1       # JWT token management
passlib[bcrypt]==1.7.4           # Password hashing (bcrypt)
email-validator==2.3.0          # Email format validation
flask-limiter==4.1.1            # Request rate limiting by IP/tier
google-auth                     # Google OAuth ID token verification

# =============================================================================
# Database (Supabase/Postgres)
# =============================================================================
psycopg2-binary==2.9.11         # Postgres driver

# =============================================================================
# Billing (Stripe) — needed for Phase B
# =============================================================================
stripe==14.4.0                  # Stripe API client

# =============================================================================
# File Storage (Cloudflare R2) — needed for Phase C
# =============================================================================
boto3                           # S3-compatible R2 client

# =============================================================================
# Environment variables
# =============================================================================
python-dotenv                   # Loads backend/.env and /opt/stemscribe/.env at startup

# =============================================================================
# Email (password resets, notifications)
# =============================================================================
resend==2.23.0                  # Resend email API

# =============================================================================
# Production Server
# =============================================================================
gunicorn                        # WSGI server for Railway deployment
